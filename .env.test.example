# Test environment configuration
# This file is used for running tests only

# Use a test API key (invalid for real requests)
OPENAI_API_KEY=test-key-for-testing

# Use fast model for testing
OPENAI_MODEL=gpt-4o-mini

# Random port for testing
PORT=0

# Minimal retries for faster tests
MAX_RETRIES=1

# Short timeout for faster test execution
TIMEOUT_MS=5000
